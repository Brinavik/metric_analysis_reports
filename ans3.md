# 三篇文献的统一 Metric Card

## 使用说明与证据等级

本文件按 `ans1.md` 的阶段 B 制作 card，但不重复三份 `*_report1.md` 已逐项记录的“指标名称、公式、统计单位、分数方向、影响因素”等信息。card 转而记录：指标组合实际服务的决策、与项目级工具辅助 agent 的契合度、证据链强度、可接受的等价答案、覆盖缺口、捷径风险、可复现性和可证伪检验。

证据等级：**L1** 为论文/报告直接说明；**L2** 为同一论文的任务、真值、失败分析交叉支持；**L3** 为基于逆向工作流的设计推断，必须标注“待专家验证”。本文件不是专家访谈结论。

---

## Card 1：CREBench

| Card 字段 | 内容 |
|---|---|
| **任务与服务场景** | 对标准密码算法的 CTF 风格二进制，完成算法识别（T1）、key/IV 提取（T2）、wrapper 级行为重实现（T3）和 checker 接受的明文恢复（T4）。它服务于“识别密码机制、恢复可用参数、复现 I/O 行为并完成受控逆向目标”的决策（L1）。|
| **metric stack 的能力模型** | T1/T2 测局部事实恢复；T3 测部分端到端语义结果；T4 测实例级终局成功；总分、perfect rate、任务相关性和条件成功率描述分阶段进展与关联（L1）。它是 Outcome-heavy 的 stack，只有 T3 对程序行为有黑盒验证；没有独立的 Semantic Model、Evidence Groundedness 或 Tool Process score（L2）。|
| **真实决策与可行动输出** | 可行动输出应至少包含：算法及模式、key/IV 及来源、输入—输出/填充/编码/派生逻辑、可运行重实现、以及解密或构造输入结果。当前提交接口只强制其中的最终字段或脚本，不强制说明“为何该结论成立”（L1/L2）。|
| **真值、oracle 与可接受等价性** | T1 允许预定义算法别名并给家族级部分分；T2 以参考 key/IV 字段严格比对；T3 在五个隐藏向量上执行提交 Python；T4 由 checker 精确接受。故 T3 对“外部可观察行为”的等价性最开放，T1/T2/T4 仍以单一规范名、字节表示或实例答案为中心（L1）。key 的字节序、字段角色或另一条等价解密路径可能因提交格式而失分，论文也观察到 T2/T4 相关性受刚性评分影响（L2）。|
| **证据链要求与可验证性** | agent 可使用二进制、Ghidra 伪代码、GDB、angr、radare2、signsrch、Python 和 Web 搜索；因此具备收集静态/动态证据的条件（L1）。但输出 schema 不要求地址、反编译片段、内存观察、调用链、trace、置信度或可复现分析步骤；评分器也不验证最终答案是否由真实二进制证据支持（L1）。T3 的隐藏测试证明若干输入上的行为一致，不能证明算法归因、密钥来源或分析过程正确（L2）。|
| **工具过程、预算与自主性** | 30 轮环境交互、600K token 上限和三次独立尝试对 agent 行为施加预算；日志分析还发现重度 GDB 使用常与停滞轨迹相关（L1）。但正式分数奖励每题三次中的最高结果，不计工具调用效率、无效调试率、信息增益、静态—动态切换质量或单次稳定性（L1/L2）。因此它测到“有限预算下可达到的最佳结果”，不是可审计的逆向生产率。|
| **鲁棒性、分布与污染控制** | 难度由优化和常量 XOR 等构造变化；论文明确未覆盖 Tigress/O-LLVM 等专业混淆（L1）。样本是 48 种标准算法、三种不安全 key 使用方式和 CTF serial-checker 形式，因此对真实协议、定制密码、加壳样本和非密码工程的外推是有限的（L1/L3）。公开算法及 CTF 结构也使原型匹配、训练记忆和工具搜索成为潜在 shortcut；“原型偏见”已被论文观察到（L1/L2）。|
| **关键遗漏的现实需求** | 关键 claim 的地址级可追溯性；key/IV 的生成与生命周期解释；wrapper 的异常/错误路径；算法或参数结论的置信度和拒答；跨样本泛化；以及把分析结果交给漏洞审计、恶意样本处置或兼容性工程的报告质量（L3，**待专家验证**其优先级）。|
| **最小补强 metric** | 在不改变 T1–T4 的前提下，增加 claim–evidence schema：每个算法、key/IV、wrapper claim 必须提供二进制位置、工具产物 ID 和解释；评分 Claim Support Rate、地址真实性、证据语义支持率、关键无证据断言率。另报告 success-vs-budget 曲线和单次成功率，而非仅 pass@3 最大值（L3，**待专家验证**具体权重和证据充分性 rubric）。|
| **可证伪检验** | (a) 用同一行为但不同算法表征/常量布局的变体，测试 T1 是否仍能反映理解而非原型匹配；(b) 构造 byte-order/IV 角色等价但提交格式不同的样本，检验 T2 的误惩罚；(c) 用超过五类输入族、异常路径和边界长度测试 T3；(d) 对答案要求可检查地址证据，比较“最终成功”与“证据支持成功”的排序差异（L3）。|

### 简要结论

CREBench **能部分证明 agent 恢复了可观察的二进制行为，但不能证明其完整理解二进制或建立了证据链**。论证是：T3 的隐藏测试和 T4 的 checker 为行为/终局结果提供了强于文本相似度的 oracle；但五个测试向量与一个 flag 都是有限观测。T1/T2 多是对参考表示的匹配，且所有任务都不要求可定位证据或过程关联。因此，高分可说明在该受控密码 CTF 分布上完成了若干结果，却不能排除搜索、记忆、原型猜测或偶然的实例级捷径（L1/L2）。

“密码逆向报告必须包含哪些证据、T3 的多大测试覆盖才足以支持实际兼容性/审计决策、以及工具效率是否应进入主分”均为 **L3，待专家验证**。

---

## Card 2：Decompile-Bench

| Card 字段 | 内容 |
|---|---|
| **任务与服务场景** | 输入函数级 binary/assembly，生成可读的 C/C++ 源码；另以二进制函数从源码库检索对应函数。它服务于“人能否理解、复用、审计或定位某一函数”的局部程序理解决策，而非完整项目的行为调查（L1/L3）。|
| **metric stack 的能力模型** | Re-Executability 评有限测试集上的功能；R2I 评 AST 特征驱动的 C 可读性；Edit Similarity、CodeSage embedding 和 CodeBLEU 评参考代码接近度；GPT-Judge 评变量名、控制流、类型恢复；Recall@1 评检索命中。Cyclomatic Complexity 与 Halstead Difficulty 是数据集复杂度审计，**不是 agent 输出能力分数**（L1）。该 stack 同时有 Outcome、表示/结构代理和主观质量层，但没有显式证据或工具过程层（L2）。|
| **真实决策与可行动输出** | 对人工逆向最有用的输出并非“像参考源码”，而是：函数边界和调用上下文、参数/返回值与副作用、关键内存访问/字段偏移、输入约束、未知项和可执行行为假设。当前主输出是孤立源码函数或检索结果；项目级调用链、证据定位和不确定性并不是必需产物（L1/L3）。|
| **真值、oracle 与可接受等价性** | 理想 oracle 是所有输入上的源/解编函数等价；实际 Re-Executability 只在有限 unit tests 上运行。它接受任何能通过测试的等价实现，但仍可能接受过拟合测试的错误程序（L1）。R2I/编辑/embedding/CodeBLEU/GPT-Judge 对参考源码或其表征有依赖；它们可给出诊断信号，不能作为等价性的独立证明（L1/L2）。优化和内联导致约四分之一抽样存在 compiler-induced scope mismatch，说明“一个二进制函数对应一个参考源码函数”的假设并不总成立（L1）。|
| **证据链要求与可验证性** | Re-Executability 提供测试 oracle，但不要求 agent 指出使程序通过的指令、分支、数据流或调用关系。可读性和相似度指标检查产物形态，不验证注释、变量名、类型或控制流解释能否由二进制证据支撑（L1/L2）。因此模型可输出看似自然的标识符或结构，而不必对真实反汇编负责（L3，**待专家验证**其在实际审计中造成的风险大小）。|
| **工具过程、预算与自主性** | 基线包含 IDA/Ghidra 和 LLM 系统，但 benchmark 的生成评测主要将反汇编文本送入模型；统一的工具调用权限、搜索/交叉引用/动态调试日志、工具预算和失败恢复协议没有报告（L1）。因此该基准更适合评估解编产物，不足以评估“工具辅助 agent 如何完成项目级逆向工作流”（L2）。|
| **鲁棒性、分布与污染控制** | 评测含 O0–O3、HumanEval/MBPP 翻译函数、2025 年后 GitHub2025、ProRec，且强调防泄漏；高优化会使部分模型显著退化（L1）。但真实 GitHub 项目的可扩展端到端重执行尚未完成，数据以公开、许可友好的 GitHub 与有限平台/语言为主，不代表商业、强混淆、多架构或项目集成问题（L1/L2）。|
| **可复现性警报** | R2I 的 AST 特征、权重和归一化细节未报告；编辑相似度的 distance→百分比分母/归一化未报告；CodeSage 的版本、池化/预处理未报告；CodeBLEU 权重未报告；GPT-Judge 的聚合、一致性校准未报告（L1，已按原文复核）。这些空缺会阻碍独立复算，也使跨工作比较不应把小数点差异作强解释。|
| **关键遗漏的现实需求** | 跨函数/跨模块数据流、库代码与自有代码区分、地址级 claim–evidence、对不可判定类型或名称的校准、动态验证策略、项目构建/依赖上下文，以及严重安全语义错误的非对称惩罚（L3，**待专家验证**任务优先级）。|
| **最小补强 metric** | 保留 Re-Executability 作为主 outcome，但扩大为多族输入、metamorphic/property tests 和异常路径；增加函数入口/范围、关键调用边、关键数据流、字段偏移的部分分；要求源码中关键语义 claim 指向反汇编地址并核验；将“测试未覆盖/类型不确定”作为可校准 abstention，而非强迫猜测（L3，**待专家验证**权重与可接受语义抽象层次）。|
| **可证伪检验** | (a) 对等价的变量重命名、控制流重构和库调用替换，观察表示相似度是否不合理波动；(b) 生成专门击穿测试集过拟合的隐藏/变形测试；(c) 在内联、尾调用和函数拆分/合并变体中审计函数配对；(d) 让人类评审同分但不同 evidence score 的结果，检验证据项是否提高实际审计效用（L3）。|

### 简要结论

Decompile-Bench **对“输出是否接近参考源码”有丰富测量，对“有限输入上是否恢复行为”有较强但不完备的测量；它不能单独证明 agent 已理解二进制并建立证据链**。论证是：Re-Executability 比 BLEU 类代理更接近语义，但其 unit tests 有限，真实项目的端到端重执行尚未成为主评测；其余核心指标多依赖参考实现或 judge 表征。再加上输入/流程未要求地址级论据，分数无法区分“可审计的理解”与“生成了恰好可通过测试、文本相近的代码”（L1/L2）。

“人类审计中可读源码、类型/命名恢复、函数检索和地址证据的相对价值”均为 **L3，待专家验证**；不应由当前指标权重替代专家偏好。

---

## Card 3：MalEval

| Card 字段 | 内容 |
|---|---|
| **任务与服务场景** | 对上游检测器送审的 Android APK，依次做安全关键函数优先级（T1）、action–asset–target 原子证据归因（T2）、行为/攻击链综合（T3）和恶意/良性及类别甄别（T4）。它服务于“是否处置、何种威胁、依据何在、是否能纠正误报且保留真阳性”的审计决策（L1）。|
| **metric stack 的能力模型** | ΔS 测高敏感 CIR 被移除后的证据退化；AE-F1 测原子证据提取/匹配；SAS 测引用函数是否在输入；EAS 测该函数是否语义支持 claim；B-F1 测行为 taxonomy；RQ 测目标、叙事、可推导性；FPCR/TPMR/F1c 测最终操作决策（L1）。三层覆盖 Outcome、Evidence 与局部信息选择，较接近审计链路；但过程仍是固定静态 CIR 协议，并非开放工具 agent（L2）。|
| **真实决策与可行动输出** | 报告可为分析师提供原子行为、支持函数、规范化行为、总体叙述、verdict 和类别（L1）。要服务真实处置，仍需明确触发条件、时间顺序、影响资产、IOC/配置、可复现步骤、置信度与相反证据；其中多数不是当前 metric 的硬性、地址级验收条件（L1/L3）。|
| **真值、oracle 与可接受等价性** | 62 份专家报告经受控结构化协议变为证据三元组、MalRadar 行为、摘要和 verdict/类别；四名研究者审计并修正部分标签。AE-F1 用受控同义等价类、加权一次一配和阈值匹配；行为与类别依赖既定 taxonomy（L1）。这比自由文本参考匹配更能容忍有限同义表达，但对新行为、不同攻击链分解、家族级报告的粒度差异和 taxonomy 外解释仍有限（L1/L2）。|
| **证据链要求与可验证性** | 此 stack 的核心贡献是把“行为正确”拆成证据存在（SAS）、证据匹配（AE-F1）和代码语义支持（EAS），并检查从证据到行为/摘要的可推导性（RQ）（L1）。但 SAS 只验证函数位于输入，不验证地址、反汇编行或动态发生；EAS/RQ 依赖 LLM judge 而非可复算的程序 oracle；攻击链是否真实可达、是否在运行时触发，当前静态 CIR 无法充分证明（L1/L2）。|
| **工具过程、预算与自主性** | Androguard、FlowDroid 风格入口点、CHA 调用图、BFS、敏感度排序和最多 300 CIR 构成固定可复现前端；模型以 temperature 0 接收已筛选 CIR 并按约束 CoT 生成报告（L1）。这能隔离推理阶段，却不测 agent 是否会选择静态/动态工具、绕过反射/动态加载、构造执行、检索 IOC 或在预算内修正假设（L1/L2）。|
| **鲁棒性、分布与污染控制** | 255 APK（230 malware、25 benign）、56 家族和六类 taxonomy；扰动 top 10% CIR 的 ΔS 可测对关键输入的局部敏感性（L1）。当前前端不覆盖 native、WebView JavaScript、动态 payload；CHA/BFS 对反射、动态加载、native callback、框架分发不完整；25 个模拟良性误报不代表真实 SOC 流量（L1）。高 ΔS 也不能排除排序器/真值共同偏置或遗漏了关键函数（L2）。|
| **可复现性警报** | EAS 和 RQ 使用 GPT-5、Gemini-2.5-Flash、DeepSeek-V3 三个异构 judge，并以 judge rationale 的平均 semantic entropy 衡量一致性；50 个随机样本的人工检查发现 judge 与人工 RQ 有 8/350 个不一致（L1）。论文仍未公开 EAS/RQ 的明确数值聚合公式、判定阈值或将 entropy 转为接受规则的方法，因此可对方向性结论有信心，不宜把其精确数值当作完全可独立复算的 oracle（L1/L2）。|
| **关键遗漏的现实需求** | 动态/原生行为发现、真实可达性与触发条件、证据的地址/trace 级可复现性、IOC 可操作性、处置时效、对抗性样本、跨版本/家族泛化和 analyst-in-the-loop 复核成本（L3，**待专家验证**优先级及阈值）。|
| **最小补强 metric** | 在 AE-F1/EAS/B-F1 之外，要求每项关键 claim 附函数地址/IR 范围与静态路径或动态 trace；以受控沙箱运行验证关键行为和触发条件；单列“未能验证”并评估置信度校准；记录从 300 CIR 到最终引用证据的覆盖与预算。对 LLM judge 用专家双标注的校准集公布阈值、一致性和置信区间（L3，**待专家验证**运行时验证成本与安全边界）。|
| **可证伪检验** | (a) 加入反射、动态加载、native bridge 和无关敏感 API 的 APK 变体，测 claim 是否仍可定位；(b) 将相同行为拆分为不同调用链/同义 action 表达，测 taxonomy/匹配稳定性；(c) 以动态 trace 作为外部 oracle，对比 EAS 高分但未发生的行为；(d) 让分析师在 blinded 条件下用报告做处置，检验 RQ/EAS 是否预测实际可用性（L3）。|

### 简要结论

MalEval 是三者中**最接近“agent 建立证据链”**的一套 metric：AE-F1、SAS、EAS 和 RQ 明确将原子 claim、支持函数、行为综合和最终 verdict 拆开，且有专家报告结构化和人工审查支撑（L1）。但它仍**不能完全证明真实二进制/恶意样本理解**：证据主要受限于静态 Dalvik CIR，EAS/RQ 依赖 LLM judge，且没有运行时可达性、地址级复现或自主工具过程 oracle。故其能证明受控静态输入中的“证据对齐和行为审计能力”强于纯分类/参考文本接近，却不能证明开放环境中的端到端恶意软件分析能力（L1/L2）。

“哪类动态证据对 SOC 决策必不可少、报告达到何种证据阈值才能升级为处置结论、以及 analyst-in-the-loop 时间成本如何计入 metric”均为 **L3，待专家验证**。

## 横向定位

| 文献 | 最强的已验证层 | 主要未证实层 | 对“理解+证据链”的当前判断 |
|---|---|---|---|
| CREBench | 有限隐藏输入上的 wrapper 行为与实例级终局成功（L1） | 地址级证据、过程有效性、广泛语义泛化（L1/L2） | 结果强于证据；不能据此证明完整理解。 |
| Decompile-Bench | 有限测试行为及参考表示的多视角接近（L1） | 证据溯源、项目级工具协作、全输入等价（L1/L2） | 兼有行为与表示代理；不能单独证明理解或证据链。 |
| MalEval | 受控静态 CIR 中的 claim–function–behavior 对齐（L1） | 动态真实性、自主工具规划、开放环境泛化（L1/L2） | 最接近证据链，但尚不是端到端真实审计证明。 |

以上横向判断均是 **L2 研究分析**，不是专家访谈结论；在将其用于正式 benchmark 设计前，应按每张 card 的可证伪检验和待专家验证项做 pilot。
