下面我按你的两个问题分别回答。我会尽量把问题说得“硬”一点：不仅重述你当前目标，也指出你初步方案中哪些地方合理、哪些地方可能把研究带偏，并给出更可执行的起步路径。

---

# 1. 你目前真正想解决的具体问题是什么？

你真正要解决的问题，不是简单地说：

> “给逆向工程 benchmark 选一个 metric。”

而是：

> **在一个真实、复杂、工具依赖强、目标开放、证据不完备的逆向工程场景中，如何为一套面向项目级二进制文件的 benchmark 设计一组具有实际效度、可实施、可复现、可区分能力差异的评估标准，使其能够可靠测量 agent 在逆向工具辅助下的项目级逆向工程能力，而不是只测量其解题、背题、模式匹配或表面文本生成能力。**

这个问题可以进一步拆成几个更具体的子问题。
 
---

## 1.1 你要评估的“逆向能力”到底是什么？

逆向工程不是一个单一任务，而是一组任务的集合。一个逆向专家面对二进制时，可能会问：

- 这个程序是干什么的？
- 哪些函数是核心功能入口？
- 程序使用了哪些协议、文件格式、系统调用、外部依赖？
- 某个函数的语义是什么？输入输出是什么？副作用是什么？
- 某个崩溃/漏洞的根因在哪里？触发路径是什么？
- 某段恶意行为的持久化、通信、提权、反调试机制是什么？
- 如果要补丁或缓解，应该修改哪里？
- 如何证明我的判断是对的？证据在哪？

所以你真正要评估的不是“会不会反汇编”，而是：

> **agent 能否在工具辅助下，围绕真实逆向目标，收集证据、建立程序语义模型、验证假设，并产出可检查、可解释、对决策有用的结论。**

这比“反编译代码像不像源码”“函数名预测准不准”“CTF flag 是否拿到”要复杂得多。

---

## 1.2 你要评估的是“项目级二进制”，不是单个函数或单题 CTF

你特别强调了 **项目级二进制文件**。这一点非常关键。

项目级二进制意味着：

- 程序规模更大；
- 存在多模块、多函数、跨过程调用；
- 可能包含第三方库、编译器运行时、打包器、混淆器产物；
- 符号可能被剥离；
- 控制流和数据流可能很复杂；
- 程序行为可能依赖环境、输入、系统调用、网络、文件系统；
- 逆向目标通常不是“解出一个 flag”，而是“形成可解释的程序理解”或“支持某个安全决策”。

因此，你的 metric 必须能处理：

- 大规模代码导航；
- 函数/模块级语义理解；
- 跨函数数据流分析；
- 结构恢复；
- 行为建模；
- 证据定位；
- 不确定性下的判断；
- 工具链协同；
- 长时间多步骤推理。

如果你最后选出的 metric 只能评价“单个函数摘要是否正确”或“是否找到 flag”，那么它就无法覆盖你真正想测的“项目级逆向能力”。

---

## 1.3 你要评估的是“工具辅助下的 agent”，不是孤立模型

你设定 benchmark 评估的是 **agent 在逆向工具辅助下的能力**。这说明你关心的不是单纯语言模型的静态知识，而是：

- agent 是否会使用工具；
- 是否能提出有效的工具调用；
- 是否能解释工具输出；
- 是否能根据工具反馈修正假设；
- 是否能从大量低层信息中提取高层语义；
- 是否能避免工具输出中的幻觉或误读；
- 是否能在预算限制下完成任务。

所以你的 metric 不能只看最终答案，也要看：

- 最终结论是否正确；
- 结论是否有证据支持；
- 工具调用是否有效；
- 推理过程是否可追踪；
- 是否存在无根据断言；
- 是否以合理成本完成任务。

也就是说，你要设计的不是一个单一指标，而是一个 **多层评估体系**。

---

## 1.4 你真正需要的是“从真实逆向需求到可验证测量”的映射

你现在的核心困难是：

> 逆向工程真实需求很复杂，但 benchmark metric 必须可计算、可复现、可自动或半自动评分。  
> 如何把前者压缩成后者，同时不丢失关键能力维度？

因此，你真正要解决的问题是：

> **如何建立一条从“真实逆向工程任务需求”到“可落地评分指标”的清晰映射链。**

这条链至少包括：

1. 真实逆向场景；
2. 逆向工程师的真实目标；
3. 完成任务所需的逆向能力；
4. 这些能力可观察的证据；
5. 可自动或半自动检查的输出形式；
6. 可复现的评分器；
7. 对评分有效性的验证。

如果你只从已有 metric 出发，很容易变成：

> “已有文献里有什么指标，我就测什么。”

但你真正需要的是：

> “真实逆向任务需要什么能力，我就想办法测什么。”

---

## 1.5 一句话重述你的问题

你真正想解决的是：

> **如何为一个面向项目级二进制文件的逆向工程 benchmark 设计一套需求驱动、证据可验证、工具使用可追踪、结果可复现、能力可区分的评估指标体系，从而有效测量 agent 在真实逆向工作流中的综合能力。**

这才是你当前的核心研究问题。

---

# 2. 你的初步行动假设是否合理？应该“自 metric 向需求”，还是“自需求向 metric”？

你的初步行动假设是：

1. 调研已有逆向 benchmark 的 metric，包括标准、实现方式、局限；
2. 扮演逆向专家，先搞清楚真实逆向场景需求，再围绕这些需求定义可落地 metric。

这两个动作都有价值，但它们的地位和顺序需要重新安排。

我的建议是：

> **主线应选自需求向 metric，但同时把已有 metric 调研作为辅助工具库、反例库和实现参考，而不是作为设计起点。**

更准确地说，不是二选一，而是：

> **需求驱动，证据约束，metric 工程化，迭代验证。**

下面详细解释。

---

# 2.1 “调研已有逆向 benchmark 的 metric”合理吗？

合理，而且必要。

原因有几个。

---

## 2.1.1 已有工作可以帮你避免重复造轮子

逆向、程序理解、二进制分析、漏洞检测、恶意软件分析、反编译等方向已经有不少评估实践。常见可测量的对象包括：

- 函数边界识别；
- 调用图恢复；
- 控制流图恢复；
- 变量/类型恢复；
- 函数命名；
- 反编译代码质量；
- 漏洞定位；
- 漏洞分类；
- 根因分析；
- 二进制补丁正确性；
- 恶意行为抽取；
- API/系统调用序列分析；
- CTF flag 获取；
- 程序问答；
- 报告质量；
- 工具调用成功率。

这些对象背后已经积累了一些 metric 形式，例如：

- accuracy / precision / recall / F1；
- exact match；
- graph similarity；
- edit distance；
- BLEU / CodeBLEU；
- pass@k；
- top-k localization；
- human evaluation；
- rubric scoring；
- LLM-as-judge；
- success rate under budget；
- tool-call validity rate。

调研这些内容可以帮你快速建立“测量原语库”。

---

## 2.1.2 已有工作能暴露常见陷阱

已有 metric 的最大价值，不只是让你学习，而是让你知道哪些做法容易失败。

例如：

### 陷阱 1：用文本相似度评价反编译质量

如果只用 BLEU、CodeBLEU、token-level similarity 评价反编译输出，会出现问题：

- 语义等价但写法不同的代码会被低分；
- 表面相似但语义错误的代码可能高分；
- 变量名、格式、注释会干扰评分；
- 编译器差异、优化等级差异会造成虚假差异。

所以你需要更语义化的评价，例如：

- 属性测试；
- 差分执行；
- 语义等价检查；
- 关键行为断言；
- 程序依赖图/数据流断言。

---

### 陷阱 2：用 flag success 代表逆向能力

CTF flag 很适合作为某些自动化漏洞利用或二进制安全任务的终点，但它有明显局限：

- 只测最终结果，不测理解过程；
- 可能通过暴力搜索、模式匹配、环境泄漏完成；
- 不要求解释；
- 不覆盖大规模项目理解；
- 容易变成解题技巧，而不是逆向能力。

所以 flag success 可以作为一类 outcome metric，但不应成为项目级逆向能力的主指标。

---

### 陷阱 3：函数名预测准确率不等于理解能力

函数名恢复是常见任务，但问题很多：

- 函数名本身可能任意；
- 编译产物中的符号可能丢失；
- 同一语义函数可有多种合理命名；
- 训练数据可能泄漏；
- 模型可能通过字符串、导入表、库名猜测，而不是真正理解语义。

因此函数名恢复可以作为辅助指标，但不能作为核心能力指标。

---

### 陷阱 4：图相似度容易脆弱

CFG/call graph similarity 看似客观，但存在问题：

- 编译器优化会改变图结构；
- 内联、尾调用、跳转表、 thunk、PLT 会影响节点和边；
- 精确图匹配过于严苛；
- 图编辑距离计算成本高且解释性有限；
- 结构相似不等于语义正确。

所以如果采用图指标，必须设计规范化、容差匹配和语义校验。

---

### 陷阱 5：人工评估有效但昂贵且不稳定

逆向报告质量、漏洞分析质量、行为摘要质量往往需要专家评价。但专家评估有缺点：

- 成本高；
- 一致性难保证；
- 容易受表达风格影响；
- 难以规模化；
- 容易被流畅但错误的文本误导。

所以人工评估适合校准和抽样审计，不适合单独作为大规模主指标。

---

## 2.1.3 已有调研的局限：容易被“可测性”牵着走

调研已有 metric 的最大风险是：

> 你会不自觉地只测已有文献里容易测的东西，而不是真实逆向任务真正需要的东西。

这就是典型的：

> “路灯效应”——只在灯下找钥匙，因为那里亮，而不是因为钥匙一定在那里。

逆向领域尤其容易这样。因为真正重要的能力，例如：

- 判断哪段代码值得深入；
- 形成可证伪假设；
- 在信息不全时合理分配分析预算；
- 识别关键数据结构；
- 理解协议状态机；
- 判断漏洞是否可达；
- 给出可复现证据链；

这些往往不好测。但不好测不代表不重要。

因此，已有 metric 调研应该是你的 **工具箱**，而不是你的 **指南针**。

---

# 2.2 “扮演逆向专家，从真实需求出发”合理吗？

非常合理，而且更适合作为主线。

原因如下。

---

## 2.2.1 benchmark 的有效性来自任务真实性

一个逆向 benchmark 是否有价值，关键不在它有没有复杂模型，而在它是否测到了真实逆向工作流中的关键能力。

如果 benchmark 脱离真实需求，可能会出现：

- 任务像算法题，不像逆向工程；
- 二进制过小，无法体现项目级难度；
- 答案唯一，逆向过程却开放；
- 只测静态模式识别，不测动态调试和假设验证；
- 只测结果，不测证据；
- 只测单点知识，不测工具协同；
- 只能区分模型记忆能力，不能区分逆向能力。

所以你需要从真实逆向需求出发，先回答：

> 一个逆向工程师/安全工程师/漏洞研究员/恶意软件分析师到底在做什么？

然后才能问：

> agent 要完成这些工作时，应该被测量哪些能力？

---

## 2.2.2 逆向工程本质上是证据驱动的

真实逆向不是“读代码并翻译”，而是：

- 提出假设；
- 用工具观察；
- 收集证据；
- 修正假设；
- 建立模型；
- 验证结论；
- 输出可行动结果。

例如：

- 判断某函数是否解析网络包；
- 判断某崩溃是否可控；
- 判断某行为是否恶意；
- 判断某补丁是否修复漏洞；
- 判断某数据流是否从输入到达危险函数。

这些都要求证据链。

因此，从需求出发会自然逼出一种更好的 metric 设计：

> 不只评最终答案，还评答案背后的证据、可验证性和工具使用过程。

---

## 2.2.3 从需求出发能避免“指标拼盘”

如果你直接从已有 metric 出发，很容易得到一个大杂烩：

- 一点函数名准确率；
- 一点 CFG F1；
- 一点 BLEU；
- 一点 flag success；
- 一点人工评分。

这些东西可能都能算，但未必共同构成一个有效能力结构。

而从需求出发，你可以先建立一个能力模型，再选择指标。例如：

- 程序结构恢复能力；
- 语义理解能力；
- 行为推理能力；
- 漏洞/缺陷定位能力；
- 证据收集能力；
- 工具使用能力；
- 报告与解释能力；
- 预算与鲁棒性。

这样最后的 metric 不是一个散乱拼盘，而是一个有结构的测量体系。

---

# 2.3 但“扮演逆向专家”也有明显缺陷

虽然这条路线更正确，但你提到“扮演逆向专家”，这里需要特别谨慎。

问题在于：

> 你不是逆向专家。  
> 仅靠研究者自己扮演专家，很容易遗漏真实工作中的隐性知识、边界情况和工具链细节。

逆向工程里有大量默会知识，例如：

- 看到某类导入表就知道可能是网络程序；
- 看到某类字符串布局就知道是 C++ 异常结构；
- 看到某类控制流模式就知道是状态机或协议解析；
- 看到某类崩溃模式就知道可能可控；
- 知道哪些反编译器输出不可信；
- 知道何时从静态分析切到动态调试；
- 知道如何绕过混淆、加壳、反调试；
- 知道怎样判断证据是否足够。

这些东西很难靠单纯设想得到。

因此，“扮演逆向专家”应升级为：

> **以专家需求访谈、真实案例复盘、工作流观察、逆向报告分析为基础，构建任务需求模型。**

你至少需要接触以下材料：

- 漏洞分析报告；
- malware analysis reports；
- reverse engineering blog posts；
- CVE advisories；
- exploit write-ups；
- patch diff analyses；
- 真实逆向工程师的工作记录；
- CTF 高质量题解；
- 安全事件复盘；
- 逆向工具使用教程；
- 专家访谈。

也就是说，你应该从“扮演专家”走向“采集专家需求”。

---

# 2.4 “自 metric 向需求”和“自需求向 metric”的对比

下面对比两种路径。

| 路径 | 优点 | 缺点 | 适用性 |
|---|---|---|---|
| 自 metric 向需求 | 起步快；容易实现；容易找到已有代码；容易发论文式 baseline | 容易测了不重要的东西；继承已有局限；任务有效性弱；可能被可计算性绑架 | 适合早期技术侦察，不适合作为主设计路线 |
| 自需求向 metric | 任务有效性更强；能定义新能力维度；更贴近项目级真实逆向；长期价值更高 | 起步难；需要领域知识；很多需求一开始不可直接测量；需要更多原型验证 | 更适合作为你的主线 |

我的判断是：

> **你应该以“自需求向 metric”为主线。**

但这并不意味着放弃已有 metric 调研。更好的形式是：

> **需求驱动设计，已有 metric 作为测量原语库和失败案例库。**

也就是：

1. 先从真实逆向需求出发；
2. 再问这些需求需要什么证据；
3. 再问这些证据能否被自动或半自动检查；
4. 再借鉴已有 metric 中可复用的实现方式；
5. 最后通过小规模原型验证指标是否真的区分能力。

---

# 3. 我建议你采用的更好初步行动方式：需求—证据—度量闭环

我不建议你先问：

> “逆向领域有哪些 metric？”

我建议你首先问：

> “真实逆向任务中，agent 需要产出什么决策？这些决策如何被验证？”

这就是一个更好的初步行动方式：

> **需求 → 决策 → 证据 → 可验证输出 → metric → 原型验证 → 修订需求**

我把它称为 **需求驱动的逆向评测设计闭环**。

---

## 3.1 第一步：定义真实逆向场景，而不是定义指标

先不要急着选 metric。先建立一个“逆向任务需求图谱”。

你可以从几类高价值场景入手。

---

### 场景 A：程序功能理解

任务示例：

- 给定一个 stripped ELF/PE 二进制，说明其主要功能；
- 找出主要模块；
- 识别协议解析、文件解析、加密、网络通信、持久化等功能；
- 给出证据。

真实需求：

- 安全分析师拿到未知样本，需要快速判断它是干什么的。

可能输出：

- 程序摘要；
- 关键函数列表；
- 功能到函数的映射；
- 证据片段，例如字符串、导入表、交叉引用、反汇编片段、动态行为。

---

### 场景 B：关键函数与关键路径定位

任务示例：

- 找出处理用户输入的核心函数；
- 找出可能导致漏洞的函数；
- 找出从入口点到危险函数的数据流路径；
- 找出某网络命令的处理函数。

真实需求：

- 漏洞研究员不会通读所有代码，而是快速定位攻击面。

可能输出：

- 函数地址；
- 函数角色；
- 路径；
- 调用链；
- 证据置信度。

---

### 场景 C：漏洞根因分析

任务示例：

- 给定崩溃信息或 PoC，找出漏洞根因；
- 判断漏洞类型；
- 给出触发路径；
- 判断是否可控；
- 提出补丁建议。

真实需求：

- 安全研究、补丁审计、漏洞复现。

可能输出：

- 漏洞类型；
- 根因函数/基本块/指令；
- 触发路径；
- 必要输入条件；
- 可利用性判断；
- 证据链。

---

### 场景 D：恶意行为分析

任务示例：

- 判断样本是否恶意；
- 提取其行为能力；
- 识别反调试、持久化、C2、加密、横向移动等能力；
- 给出 IOC 或行为证据。

真实需求：

- 恶意软件分析师需要快速形成行为画像。

可能输出：

- 行为类别；
- 相关函数；
- API/系统调用；
- 字符串/配置；
- 网络行为；
- 反分析机制。

---

### 场景 E：补丁/版本差异分析

任务示例：

- 给定两个版本二进制，判断修复了什么；
- 判断是否为安全补丁；
- 定位补丁点；
- 推断漏洞类型。

真实需求：

- 补丁逆向、漏洞挖掘、供应链安全。

可能输出：

- 差异函数；
- 修改语义；
- 是否修复边界检查；
- 可能漏洞类型；
- 证据。

---

### 场景 F：二进制级问答与报告生成

任务示例：

- 回答关于程序语义的问题；
- 生成逆向报告；
- 对特定函数进行解释；
- 根据报告支持人工决策。

真实需求：

- 逆向工程师经常需要向团队解释分析结果。

可能输出：

- 结构化 QA；
- 报告；
- claim-evidence 对；
- 置信度。

---

## 3.2 第二步：从场景中抽取“决策”

每个真实逆向任务背后都有一个决策。

例如：

| 场景 | 真实决策 |
|---|---|
| 功能理解 | 这个程序是否值得深入分析？它可能是什么？ |
| 关键函数定位 | 我应该优先逆向哪些函数？ |
| 漏洞分析 | 崩溃是否安全相关？根因在哪里？ |
| 恶意软件分析 | 样本是否恶意？风险等级如何？需要哪些处置？ |
| 补丁分析 | 补丁是否修复安全问题？漏洞可能是什么？ |
| 报告生成 | 我的结论是否有足够证据？是否能被同行接受？ |

benchmark 的任务设计应该围绕这些决策，而不是围绕抽象指标。

因为指标只是手段，决策有效性才是目的。

---

## 3.3 第三步：定义完成任务所需的“能力原子”

从需求出发，你可以把项目级逆向能力拆成一组可观察的能力原子。

例如：

### 结构恢复能力

- 识别函数边界；
- 识别调用关系；
- 识别基本块；
- 识别跳转表；
- 识别数据段中的结构体；
- 区分代码和数据；
- 识别库代码和自有代码。

### 语义理解能力

- 推断函数参数；
- 推断返回值；
- 推断局部变量；
- 推断结构体字段；
- 推断函数副作用；
- 推断输入约束；
- 推断状态机。

### 行为推理能力

- 根据输入推断执行路径；
- 推断系统调用序列；
- 推断网络行为；
- 推断文件操作；
- 推断持久化行为；
- 推断反调试/反沙箱行为。

### 漏洞分析能力

- 识别危险操作；
- 追踪不可信输入；
- 判断边界检查缺失；
- 判断整数溢出、格式串、UAF、堆溢出等可能性；
- 判断可达性和可控性。

### 工具使用能力

- 选择合适工具；
- 构造有效命令；
- 解析工具输出；
- 处理工具失败；
- 在静态/动态分析之间切换；
- 使用搜索、交叉引用、断点、trace、符号执行等。

### 证据组织能力

- 给出地址级证据；
- 引用反汇编/反编译片段；
- 给出调用链；
- 给出动态 trace；
- 给出可复现步骤；
- 区分确定事实和猜测。

### 预算与不确定性管理能力

- 在大规模二进制中优先分析高价值区域；
- 不陷入无关细节；
- 在证据不足时表达不确定性；
- 避免过度自信；
- 在时间/调用预算内完成任务。

这些能力原子会决定你的 metric 不应该只有一个总分，而应有多维指标。

---

# 4. 我建议你设计的不是一个 metric，而是一个 metric stack

对于你的目标，单一指标几乎一定不够。

更合理的是设计一个分层指标体系。

我建议你考虑以下五层。

---

## 4.1 第一层：任务结果层，Task Outcome

这一层回答：

> agent 是否完成了逆向任务的最终目标？

示例指标：

- Verified Success Rate，验证成功率；
- Root-cause Localization Accuracy，根因定位准确率；
- Answer Accuracy，问答准确率；
- Patch Correctness，补丁正确性；
- Behavior Extraction Completeness，行为抽取完整度。

特点：

- 最接近最终结果；
- 容易解释；
- 但容易忽略过程和证据。

实现方式：

- 对每个任务定义 oracle；
- 输出必须结构化；
- 自动判定或半自动判定；
- 对部分正确给部分分。

例子：

| 任务 | 输出 | 评分 |
|---|---|---|
| 找漏洞函数 | 函数地址列表 | top-1/top-5 hit rate |
| 找触发路径 | 调用链 | path F1 或 path edit distance |
| 程序功能摘要 | 结构化标签 | 标签 precision/recall |
| 恶意行为分析 | 行为集合 | behavior F1 |
| 补丁建议 | 二进制补丁或源码级建议 | 在测试环境中验证是否修复 |

注意：对于项目级逆向，不应只使用 strict exact match。真实逆向常有多解、近似解、不同抽象层次的答案，所以需要部分分和容差。

---

## 4.2 第二层：语义模型层，Semantic Model Quality

这一层回答：

> agent 是否建立了正确的程序结构/语义模型？

示例对象：

- 函数边界；
- 调用图；
- 控制流图；
- 数据流；
- 变量；
- 结构体；
- 类型；
- 系统调用关系；
- 输入到危险函数的数据流；
- 协议状态机。

示例指标：

- Function Boundary F1；
- Call Graph Edge F1；
- Variable Recovery F1；
- Type Field Offset F1；
- Data-flow Edge F1；
- Struct Recovery Score；
- Semantic Predicate Accuracy。

特点：

- 比最终答案更细；
- 可以诊断模型为什么失败；
- 但需要仔细处理编译器/优化工具带来的结构差异。

实现建议：

- 不要直接用源码标签；
- 要把源码真值转换成二进制级实体；
- 对函数名、变量名做规范化；
- 对调用图做 PLT/thunk/tail-call 规范化；
- 对 CFG 做语义等价化；
- 对结构体字段按偏移和大小匹配，而不是按名字匹配。

例如：

### 函数边界

预测：函数起始地址和范围。

评分：

- 严格匹配；
- 或按 IoU 给部分分；
- 或按函数入口匹配加范围重叠率。

### 调用图

预测：调用边集合。

评分：

- precision / recall / F1；
- 可选地忽略某些编译器生成边；
- 可选地允许间接调用以保守方式匹配。

### 结构体恢复

预测：

```json
{
  "struct": "conn_state",
  "size": 64,
  "fields": [
    {"offset": 0, "size": 8, "kind": "ptr"},
    {"offset": 8, "size": 4, "kind": "int"}
  ]
}
```

评分：

- 结构体大小是否正确；
- 字段偏移是否正确；
- 字段类型族是否正确；
- 字段用途是否可由语义断言验证。

---

## 4.3 第三层：证据与事实层，Evidence Groundedness

这一层回答：

> agent 的结论是否有二进制层面的证据支持？

这对逆向工程尤其重要。

因为逆向报告最常见的问题不是“没答案”，而是“答案听起来合理，但没有证据”。

示例输出格式：

```json
{
  "claim": "sub_401A30 is the network command dispatcher.",
  "evidence": [
    {
      "type": "disassembly",
      "location": "0x401A30-0x401A9F",
      "reason": "switch table over command IDs"
    },
    {
      "type": "xref",
      "from": "0x4017E2",
      "to": "0x401A30",
      "reason": "called after recv loop"
    },
    {
      "type": "string",
      "value": "CMD_LOGIN",
      "location": "0x4C2010"
    }
  ],
  "confidence": 0.82
}
```

可测指标：

- Claim Support Rate：有多少关键声明有证据；
- Evidence Precision：证据是否真的支持声明；
- Evidence Recall：关键结论是否遗漏重要证据；
- Hallucinated Address Rate：虚构地址/函数/字符串比例；
- Unsupported Critical Claim Rate：无证据关键断言率；
- Evidence Locality：证据是否指向正确函数/地址范围。

这一层非常关键，因为它可以抑制：

- 流畅但无根据的报告；
- 靠训练数据记忆猜题；
- LLM 幻觉；
- “看似专业”的泛泛而谈。

---

## 4.4 第四层：过程与工具使用层，Process and Tool Use

这一层回答：

> agent 的逆向过程是否有效、合理、可追踪？

因为你的对象是工具辅助下的 agent，所以不能只看最终输出。

可记录的过程数据：

- 工具调用序列；
- 命令参数；
- 命令是否成功；
- 查询了哪些地址/函数/符号；
- 是否使用交叉引用；
- 是否使用动态调试；
- 是否使用字符串搜索；
- 是否使用控制流分析；
- 是否重复无效调用；
- 是否根据工具输出改变假设；
- 最终答案是否引用了过程中获取的信息。

可设计指标：

- Valid Tool Call Rate：有效工具调用比例；
- Tool Coverage：是否使用了任务所需工具类型；
- Tool Efficiency：达到证据所需调用次数；
- Redundant Call Rate：重复/无效调用率；
- Information Gain Proxy：后续结论中被引用的证据来自哪些调用；
- Strategy Switch Score：是否在静态分析失败后切换动态分析；
- Exploration Precision：是否集中分析任务相关区域；
- Budget-aware Success：在给定时间/调用预算下的成功率。

注意：过程指标容易被 gaming。例如 agent 可以疯狂调用工具刷高“工具使用率”。所以过程指标必须和结果、证据绑定。

例如：

> 不只看“调用了多少次”，而看“哪些调用产出了最终被引用且验证正确的证据”。

---

## 4.5 第五层：鲁棒性、可靠性和安全层，Robustness and Reliability

项目级二进制往往有各种干扰。

你需要测：

- 剥离符号后是否仍能分析；
- 不同优化等级下是否稳定；
- 混淆后是否退化过度；
- 加壳/保护后是否能识别并处理；
- 多架构/多平台是否稳定；
- 面对大型二进制是否扩展；
- 是否会被无关字符串误导；
- 是否会输出危险可执行攻击代码；
- 是否在证据不足时承认不确定。

指标示例：

- Performance Drop under Stripping；
- Performance Drop under Obfuscation；
- Calibration Error：置信度与准确率是否匹配；
- Abstention Quality：在无法判断时是否合理放弃；
- Safety Violation Rate：是否生成越界危险操作；
- Reproducibility：相同输入下结果是否稳定。

---

# 5. 已有逆向 benchmark metric 的主要类型、实现方式与局限

你在调研时，可以按照下面这个框架整理已有工作。

---

## 5.1 反编译/代码重建类

常见任务：

- 从二进制恢复类 C 代码；
- 从汇编恢复高级语言代码；
- 恢复函数体。

常见指标：

- exact match；
- BLEU；
- CodeBLEU；
- AST similarity；
- compile-and-test pass rate；
- semantic equivalence。

局限：

- 文本相似不等于语义正确；
- 编译/测试等价成本高；
- 不同反编译器风格差异大；
- 对真实逆向工作不一定关键；
- 项目级程序中完整反编译往往不是逆向工程师的主要目标。

建议：

- 如果你的目标不是评测 decompiler agent，不要把反编译文本相似度作为主指标；
- 如果要用，最好用行为测试、属性测试或语义断言。

---

## 5.2 函数/结构恢复类

常见任务：

- 函数边界识别；
- 函数名预测；
- 调用图恢复；
- 变量/类型恢复。

常见指标：

- precision / recall / F1；
- graph edit distance；
- type accuracy；
- name exact match / normalized match。

局限：

- 编译器转换导致真值不稳定；
- 函数名和变量名不是自然真值；
- 图匹配对优化敏感；
- 这些指标更偏静态程序分析，不一定反映高层语义理解。

建议：

- 把它们作为中间能力指标；
- 建立二进制级规范真值；
- 加入语义验证任务，避免只恢复结构不理解行为。

---

## 5.3 漏洞/崩溃分析类

常见任务：

- 漏洞检测；
- 漏洞分类；
- 根因定位；
- 可达性分析；
- 可利用性判断；
- 补丁生成。

常见指标：

- detection accuracy；
- top-k localization；
- CWE classification accuracy；
- root cause function accuracy；
- patch pass rate；
- false positive rate。

局限：

- 漏洞标签常常来自源码，可能泄漏源码信息；
- 二进制层根因可能与源码层不完全对应；
- 可利用性判断高度依赖环境；
- 补丁验证复杂；
- 数据易污染。

建议：

- 对漏洞任务，强调“证据链 + 触发路径 + 可验证修复”；
- 不要只测“是否存在漏洞”的二分类。

---

## 5.4 恶意软件/行为分析类

常见任务：

- 恶意家族分类；
- 行为能力抽取；
- API call sequence prediction；
- IOC extraction；
- config extraction。

常见指标：

- classification accuracy；
- behavior F1；
- sequence edit distance；
- IOC precision/recall；
- config field accuracy。

局限：

- 家族标签可能粗糙；
- 动态行为依赖沙箱环境；
- 行为标签体系不统一；
- 只分类不解释，容易忽略证据；
- 对抗样本下泛化难。

建议：

- 将行为抽取设计成结构化、可验证的 claim-evidence 任务；
- 加入反调试、环境感知、配置解密等真实挑战。

---

## 5.5 CTF / exploit / puzzle 类

常见任务：

- 找到 flag；
- 构造输入；
- 绕过保护；
- 利用漏洞。

常见指标：

- success rate；
- time to solve；
- number of attempts；
- exploit reliability。

局限：

- 往往偏单点解题；
- 不一定代表项目级逆向；
- 可能不要求完整程序理解；
- 容易受到题库污染；
- 成功与否可能受环境细节影响。

建议：

- 可以作为子任务或压力测试；
- 不宜作为项目级逆向能力主指标。

---

## 5.6 程序问答/报告类

常见任务：

- 回答关于二进制的问题；
- 生成函数摘要；
- 生成分析报告。

常见指标：

- QA accuracy；
- exact match；
- LLM-as-judge；
- human evaluation；
- rubric score。

局限：

- 问答题可能过于碎片化；
- 自由文本评分不稳定；
- LLM judge 可能偏袒流畅文本；
- 难以判断是否有真实证据；
- 可能被训练数据中的相似文本污染。

建议：

- 使用结构化输出；
- 强制 claim-evidence；
- 自动验证证据地址；
- LLM judge 只作辅助，不作唯一裁判；
- 人工评估用于校准。

---

# 6. 你应该如何设计可落地的 metric implementation？

我建议你采用一个固定模板：

> **每个任务 = 场景 + 决策 + 输出 schema + ground truth/oracle + 评分器 + 反作弊设计。**

---

## 6.1 输出必须结构化

不要让 agent 只输出一段自由文本。

至少要求它输出结构化 JSON，例如：

```json
{
  "task_id": "bin_001",
  "summary": "The binary is a custom TCP service that parses length-prefixed commands.",
  "key_functions": [
    {
      "address": "0x401A30",
      "role": "command_dispatcher",
      "confidence": 0.9,
      "evidence": ["xref:0x4017E2", "string:CMD_LOGIN"]
    }
  ],
  "claims": [
    {
      "id": "c1",
      "statement": "Input length is parsed at 0x4018B0 and used without upper bound.",
      "type": "dataflow",
      "locations": ["0x4018B0", "0x401904"],
      "confidence": 0.7
    }
  ],
  "answers": {
    "vulnerable_function": "0x401904",
    "vulnerability_type": "heap_overflow",
    "trigger_path": ["main", "handle_packet", "copy_payload"]
  }
}
```

结构化输出的好处：

- 可自动评分；
- 可检查幻觉；
- 可追踪证据；
- 可部分给分；
- 可做误差分析。

---

## 6.2 真值来源要分层

项目级二进制很难有完美真值。你需要组合多种真值来源。

### 真值类型 1：构建时真值

如果你有源码，可以编译出二进制，并保留：

- 函数地址；
- 调用图；
- 类型布局；
- 调试信息；
- 测试用例；
- 插桩信息。

注意：

- 不要把源码变量名直接当真值；
- 要转换成二进制层实体；
- 要对优化和混淆做规范化。

---

### 真值类型 2：动态执行真值

通过运行程序收集：

- 系统调用序列；
- 函数执行轨迹；
- 覆盖率；
- 输入输出；
- 网络行为；
- 文件行为；
- 崩溃点；
- 漏洞触发路径。

优点：

- 更贴近行为；
- 可以验证动态结论。

难点：

- 环境依赖；
- 路径覆盖不完整；
- 需要沙箱和 oracle。

---

### 真值类型 3：专家标注真值

对于高层语义，例如：

- 程序功能；
- 漏洞根因；
- 恶意行为；
- 报告质量；

需要专家标注。

建议：

- 多名专家独立标注；
- 记录分歧；
- 使用 rubric；
- 只把高一致性部分作为自动评测金标；
- 分歧部分用于人工审计或校准模型。

---

### 真值类型 4：属性/断言真值

不一定要求唯一答案，而可以验证一组属性。

例如：

- 某函数必须调用 `recv`；
- 某输入必须导致某函数被执行；
- 某路径必须经过边界检查；
- 某结构体字段偏移必须为 16；
- 某命令处理函数必须响应特定字符串。

这种方式很适合逆向，因为真实逆向答案常常不是唯一文本，而是一组可验证事实。

---

## 6.3 评分器要支持部分分和不确定性

真实逆向不是全对或全错。

例如：

- 找对了漏洞函数但路径不完整；
- 找对了核心函数但证据不足；
- 找对了行为但置信度过高；
- 找错了根因但摘要部分正确；
- 输出有 80% 正确，但有一个关键幻觉。

所以评分应该包括：

- partial credit；
- critical error penalty；
- confidence calibration；
- evidence bonus；
- hallucination penalty。

一个简单但可用的复合思路：

```text
TaskScore =
    w1 * VerifiedOutcomeScore
  + w2 * EvidenceGroundednessScore
  + w3 * StructuralSemanticScore
  - w4 * HallucinationPenalty
  - w5 * UnsupportedCriticalClaimPenalty
```

但权重不要一开始拍脑袋决定。应通过小规模 pilot 数据校准。

---

## 6.4 引入预算感知指标

真实逆向有成本约束。

你可以设置不同预算：

- 工具调用次数预算；
- 时间预算；
- token 预算；
- 动态执行次数预算；
- 人工查询预算。

然后测：

- success rate at budget B；
- area under success-rate vs budget curve；
- evidence acquired per tool call；
- time to first correct key function；
- cost-normalized score。

例如：

| 预算 | 指标 |
|---|---|
| 低预算 | 能否快速找到关键入口？ |
| 中预算 | 能否建立基本语义模型？ |
| 高预算 | 能否完成深度根因分析？ |

这能更好地区分“真正高效逆向”和“靠大量无效搜索碰运气”。

---

# 7. 一个可能的评测指标框架草案

下面给出一个可以直接拿来讨论的草案。

---

## 7.1 一级维度

建议设置 6 个一级维度：

1. **Outcome Correctness**：最终任务是否完成；
2. **Semantic Recovery**：程序结构和语义模型是否正确；
3. **Evidence Groundedness**：结论是否有可验证证据；
4. **Tool-use Effectiveness**：工具使用是否有效；
5. **Efficiency and Budget Awareness**：是否在合理成本下完成；
6. **Reliability and Safety**：是否稳定、低幻觉、安全。

---

## 7.2 指标表草案

| 维度 | 指标 | 说明 | 实现方式 | 风险 |
|---|---|---|---|---|
| Outcome | Verified Success Rate | 主任务目标是否通过 oracle | 自动 validator | 任务设计不全会偏 |
| Outcome | Top-k Localization | 关键函数/路径是否命中 | 地址/函数匹配 | 对多解要容差 |
| Outcome | Behavior F1 | 行为集合抽取质量 | 结构化标签匹配 | 标签体系要稳定 |
| Semantic | Function Boundary F1 | 函数边界恢复 | IoU/入口匹配 | 优化敏感 |
| Semantic | Call Graph F1 | 调用图恢复 | 规范化后边匹配 | 间接调用难 |
| Semantic | Type/Struct Score | 结构体字段恢复 | 偏移/大小/类型族匹配 | 真值复杂 |
| Evidence | Claim Support Rate | 关键声明是否有证据 | claim-evidence 校验 | 证据质量需审 |
| Evidence | Hallucination Rate | 虚构地址/函数/字符串比例 | 自动地址检查 | 需要完整二进制镜像 |
| Evidence | Evidence Recall | 是否提供关键证据 | 专家标注关键证据集 | 标注成本高 |
| Tool Use | Valid Tool Call Rate | 工具调用是否合法有效 | 工具日志分析 | 可能鼓励多调用 |
| Tool Use | Evidence-producing Call Rate | 产出被引用证据的调用比例 | trace 与 claim 关联 | 关联算法要稳 |
| Tool Use | Redundancy Rate | 重复无效调用比例 | trace 聚类/去重 | 定义需合理 |
| Efficiency | Budget-aware Success | 不同预算下成功率 | 曲线或 AUC | 预算设置需校准 |
| Reliability | Calibration Error | 置信度与准确率匹配度 | ECE 等 | agent 可能乱报置信度 |
| Safety | Unsafe Output Rate | 危险利用代码/操作 | 规则/人工审计 | 需明确安全边界 |

---

# 8. 你尤其要防止的几个研究陷阱

---

## 8.1 不要把“容易自动评分”当成“重要”

例如函数名匹配容易自动评分，但函数名不是逆向的核心。

真正核心但难测的包括：

- 哪里值得看；
- 哪个假设应该被验证；
- 哪个证据足以支撑结论；
- 哪个函数是协议状态机核心；
- 哪条数据流真正危险。

你需要优先测重要的，再想办法让它可测，而不是只测容易的。

---

## 8.2 不要把 benchmark 做成“二进制版问答数据集”

如果题目都是：

> “这个函数做了什么？”

并且答案是短文本，那么很容易退化成：

- 模式识别；
- 训练集记忆；
- 表层匹配。

项目级逆向应该包含：

- 多步工具调用；
- 证据收集；
- 动态验证；
- 跨函数推理；
- 大规模导航；
- 预算约束。

---

## 8.3 不要只评最终答案，不评证据

否则你会奖励“猜对”的模型，而不是“理解”的模型。

逆向工程的可信度来自证据链。

所以一定要引入：

- claim；
- evidence；
- location；
- tool trace；
- confidence；
- verification result。

---

## 8.4 不要让真值过度依赖源码

虽然源码可以用来生成真值，但逆向任务的对象是二进制。

如果你直接用源码函数名、变量名、行号作为标准，可能会测成：

> “从二进制猜源码作者命名风格”。

你应该把源码真值转换成：

- 二进制地址；
- 函数范围；
- 调用边；
- 字段偏移；
- 类型大小；
- 动态行为；
- 语义断言。

---

## 8.5 不要忽视污染和记忆问题

如果二进制来自公开项目、公开漏洞库、公开 CTF，agent 可能已经见过。

缓解方式：

- 使用新生成二进制；
- 对开源项目进行变体编译；
- 使用私有样本；
- 对样本做等价变换；
- 加入动态随机化配置；
- 使用未公开测试集；
- 对训练截止后的样本做特殊标记；
- 检测输出是否过度依赖记忆而非工具证据。

---

# 9. 我建议你选择哪种初步行动？

如果必须在你给出的两个方向中选一个：

> **我建议选择“自需求向 metric”。**

理由如下：

1. 你的目标是项目级真实逆向，不是已有 benchmark 的简单扩展；
2. 已有 metric 多数服务于较窄任务，不一定覆盖真实工作流；
3. 逆向能力的有效性必须由任务和决策定义，而不是由已有指标定义；
4. 你需要先建立能力模型，再选择测量方式；
5. 这样设计出的 benchmark 更可能具有长期影响力和领域认可度。

但是，你也不能完全放弃已有 metric 调研。

更好的策略是：

> **以“自需求向 metric”为主轴，以“已有 metric 调研”为支撑、对照和实现参考。**

也就是说：

- 不要让已有 metric 决定你要测什么；
- 但要让已有 metric 告诉你哪些测量方法可行、哪些不可行；
- 同时把已有 metric 的局限作为你的创新点来源。

---

# 10. 我建议你下一步怎么做？

我建议你接下来不要先写 metric 公式，而是做一个 **最小可验证评测原型**。

---

## 10.1 第一步：选 3 个代表性任务

不要一开始覆盖所有逆向场景。先选 3 个最能代表项目级逆向的任务。

我建议优先选择：

### 任务 1：关键函数定位与功能解释

例如：

> 在一个 stripped 二进制中，找出处理网络命令的核心函数，并解释证据。

这个任务考察：

- 导航；
- 交叉引用；
- 字符串/导入表利用；
- 函数角色判断；
- 证据组织。

---

### 任务 2：输入到危险函数的数据流追踪

例如：

> 判断外部输入是否到达某个危险操作，并给出路径。

这个任务考察：

- 数据流；
- 调用链；
- 动态/静态结合；
- 漏洞分析基础。

---

### 任务 3：根因定位与报告

例如：

> 给定崩溃或异常行为，定位根因函数/路径，并输出结构化报告。

这个任务考察：

- 多步推理；
- 证据链；
- 不确定性；
- 报告质量；
- 工具使用。

这三个任务能形成一个不错的最小闭环。

---

## 10.2 第二步：为每个任务写一个“评测卡片”

每个任务用统一模板描述。

模板示例：

```text
Task Name:
Scenario:
Real-world decision:
Input artifacts:
Allowed tools:
Time/tool budget:
Required output schema:
Ground truth source:
Automatic validator:
Human audit components:
Possible failure modes:
Possible gaming strategies:
```

这会逼迫你把需求转成可测对象。

---

## 10.3 第三步：设计输出 schema

例如任务 1 的输出可以是：

```json
{
  "key_functions": [
    {
      "address": "0x...",
      "role": "command_dispatcher",
      "confidence": 0.0,
      "evidence": [
        {
          "type": "string|xref|disasm|dynamic_trace|import",
          "location": "0x...",
          "description": "..."
        }
      ]
    }
  ]
}
```

任务 2 的输出可以是：

```json
{
  "reachable": true,
  "source": "0x...",
  "sink": "0x...",
  "paths": [
    {
      "functions": ["0x...", "0x...", "0x..."],
      "conditions": ["length < 0x100", "cmd == 3"],
      "evidence": ["trace_id:...", "xref:..."]
    }
  ]
}
```

任务 3 的输出可以是：

```json
{
  "root_cause": {
    "function": "0x...",
    "basic_block": "0x...",
    "type": "heap_overflow",
    "confidence": 0.0
  },
  "trigger_path": [],
  "evidence": [],
  "uncertainty": "..."
}
```

---

## 10.4 第四步：先做自动可验证部分，再做人工评估部分

优先级建议：

1. 地址是否正确；
2. 函数是否命中；
3. 路径是否存在；
4. 证据地址是否真实；
5. 行为标签是否匹配；
6. 动态验证是否通过；
7. 报告质量人工评估；
8. 证据充分性人工抽检。

也就是说，先做能自动检查的硬指标，再引入专家评估作为校准。

---

## 10.5 第五步：用少量 agent 和人类基线做 pilot

找：

- 1—2 个当前 agent；
- 1—2 个人类逆向基线；
- 3—5 个二进制样本；
- 3 类任务；
- 若干预算设置。

然后观察：

- 哪些指标区分度高；
- 哪些指标噪声大；
- 哪些任务无法自动评分；
- 哪些证据难以验证；
- agent 如何 gaming；
- 人类是否觉得任务真实；
- 指标是否反映真实能力差异。

这一步非常关键。没有 pilot，metric 很容易只是纸面合理。

---

# 11. 一个更完整的研究表述

你可以把你的研究问题表述为：

> 本项目旨在设计一个面向项目级二进制文件的逆向工程 benchmark，用于评估 AI agent 在逆向工具辅助下的程序理解、证据收集、漏洞/行为分析、结构化推理与报告能力。核心研究问题不是寻找单一评分指标，而是构建一套从真实逆向需求出发、以可验证证据为基础、兼容自动评分与专家审计的多层评估体系。该体系应能区分真正具备逆向工程能力的 agent 与仅依赖模式匹配、训练记忆或表面文本生成的 agent。

这个表述比较准确地概括了你当前方向。

---

# 12. 最终建议总结

你的初步行动假设整体方向是对的，但需要调整优先级。

## 你的两个初步行动

### 行动一：调研已有逆向 benchmark metric

合理，但不要让它主导设计。

它应该服务于：

- 了解可用测量原语；
- 识别已有局限；
- 避免重复实现；
- 寻找自动评分技术；
- 发现失败模式。

但不应该让它决定：

- 你的任务定义；
- 你的能力模型；
- 你的核心评测目标。

---

### 行动二：从真实逆向需求出发定义 metric

这是更正确的主线。

但需要升级：

- 不要只靠自己扮演专家；
- 要收集真实案例、报告、专家访谈；
- 要把需求转成任务；
- 把任务转成决策；
- 把决策转成证据；
- 把证据转成可验证输出；
- 再通过 pilot 验证指标有效性。

---

## 我的建议路线

> **采用“自需求向 metric”的主线，同时建立已有 metric 调研作为辅助工具库。**

具体路线：

```text
真实逆向场景
  → 真实决策需求
  → 能力原子分解
  → 任务设计
  → 结构化输出
  → 证据与 oracle
  → 自动/半自动 metric
  → pilot 验证
  → 修订能力模型与指标
```

而不是：

```text
已有 metric
  → 找能套用的任务
  → 拼成 benchmark
```

后者容易做出来一个“能算分但不测真实能力”的 benchmark。

---

# 13. 最简短结论

如果只给出一句结论：

> 你真正要解决的是：如何为项目级二进制逆向 benchmark 设计一套能测量 agent 在工具辅助下真实逆向能力的多层评估体系；  
> 你应优先选择“自需求向 metric”的路径，但同时把已有 metric 调研作为实现参考和反例库，并通过“需求—证据—可验证输出—原型验证”的闭环来落地。