# 阶段 D：把 metric 分析变成可检验命题与最小验证方案

## 1. 阶段目标与结论边界

阶段 D 不再增加新的指标清单，而是检验阶段 B 的三个判断：

1. 结果分数是否测到真实任务，而不是最终答案、参考文本或标签捷径；
2. 语义模型是否由二进制证据支撑，并且能区分“猜对”与“理解”；
3. 工具过程是否在给定预算内有效、可复现，而不是只取最好的一次结果。

沿用阶段 C 的最小 stack：

```text
核心面：O = Verified Task Outcome
       S+E = Evidence-Grounded Semantic Model
       P = Effective Process under Budget
横切轴：R = Robustness/Reliability
       V = Validity/Reproducibility
```

本阶段输出的是**可证伪的测量假设和 pilot 设计**，不是已经经过专家确认的行业阈值。文中的“应当”表示待验证设计假设；“文献直接支持”沿用 L1，交叉分析为 L2，工作流推断为 L3。

## 2. 统一检验协议

### 2.1 每个命题的最小记录

每个命题都必须预先登记以下字段，避免在看到结果后改写解释：

| 字段 | 要求 |
|---|---|
| 被检验构念 | O、S+E、P、R 或 V 中的一个或多个面 |
| 变体/对照 | 只改变一个已知因素；保留二进制、输入和环境的其余部分 |
| 预测 | 若 metric 有效，分数、排序或错误类型应如何变化 |
| 反事实预测 | 若 metric 是错误代理，可能出现什么相反结果 |
| 观测量 | 原始分项、总分、成本、证据字段和人工标签，不只记录加权总分 |
| 判据 | 预先定义效应方向、最小效应量、置信区间和失败阈值 |
| 复核 | 至少两名独立专家盲评；记录分歧和裁决理由 |

所有运行保存：样本版本、编译器/优化参数、输入哈希、工具版本、agent 轨迹、调用时间、token/回合预算、随机种子、结构化输出、原始日志及评分器版本。

### 2.2 通用输出 schema

为了让三类任务可以比较，要求 agent 对每个关键结论输出同一类字段；场景特定字段作为 `task_fields` 扩展：

```json
{
  "claim_id": "...",
  "claim": "...",
  "semantic_object": "function|edge|dataflow|algorithm|behavior|verdict",
  "locations": ["binary address/range or function id"],
  "evidence": [{"kind": "disassembly|xref|trace|test|config|diff", "id": "..."}],
  "conditions": ["input/state/environment condition"],
  "confidence": 0.0,
  "status": "verified|supported|uncertain|abstain",
  "task_fields": {}
}
```

这不是要求三篇原论文回溯重做全部工作，而是 pilot 中新增的最小可审计接口。没有地址或可复算产物的自由文本只能计入叙述诊断，不能计入 S+E 的充分成功。

### 2.3 统一比较方式

- 对每个样本保留低、中、高三档预算，而不是只报告最高预算或 `pass@3`。
- 报告 `success-at-budget` 曲线、预算归一化面积（AUC）和单次运行方差；最高分只作为附加指标。
- 对关键错误使用非对称惩罚：虚构地址、错误恶意 verdict、错误 key/IV 来源和错误漏洞结论的代价高于遗漏非关键描述。
- 不把不同任务的原始分数直接相加。跨任务仅比较标准化后的效应方向、专家排序相关和是否通过最低门槛。

## 3. Decompile-Bench：可证伪命题

### D-D1：参考表示指标错误惩罚语义等价结果

**命题（L1/L2 推出，L3 待验证）**：若 Edit Similarity、CodeBLEU、embedding 或 R2I 主要测参考表示接近度，则仅改变变量名、控制流结构、等价库调用或类型注释时，行为 outcome 不变，但表示分数会出现不应有的显著下降；专家对可审计性的判断应更接近行为和证据分数，而非文本分数。

**最小实验**：从 5 个函数各生成四种语义等价变体：变量/函数重命名、`if` 与早返回重写、等价循环/递归、标准库调用替换；使用原始测试加 metamorphic/property tests。盲评者只看二进制、候选源码和证据字段，不看 metric 分数。

**支持/反驳判据**：

- 支持：行为通过率和专家“可接受”率近似不变，而至少两种表示指标下降；
- 反驳：表示指标对等价变体保持稳定，且比行为/证据指标更能预测专家排序；
- 记录变体间分数差、专家排序 Spearman/Kendall 相关和 95% bootstrap CI。

### D-D2：有限 unit tests 会奖励过拟合

**命题**：仅用公开 unit tests 的 Re-Executability 会接受在测试集上通过、但在边界、异常或未覆盖分支上错误的候选；加入隐藏输入族和属性断言后，O 与专家判断的区分力应提高。

**最小实验**：每个函数保留公开测试，另生成三类隐藏测试：边界长度/数值、异常路径、随机 metamorphic 变换；构造一个“测试过拟合”候选（显式记忆公开输入）和一个真实等价候选。比较公开测试分数、隐藏测试分数、证据分数和专家判定。

**支持/反驳判据**：

- 支持：过拟合候选公开通过但隐藏测试显著失败；隐藏 O 对专家可接受性 AUC 高于公开 O；
- 反驳：公开测试已能稳定排除过拟合，或隐藏测试与专家排序无增益；
- 必须报告测试族覆盖、生成器种子和误杀真实等价候选的比例。

### D-D3：函数级参考配对在优化/内联下不稳定

**命题**：O2/O3、尾调用、内联和函数拆分/合并会破坏“一段二进制对应一个源码函数”的假设；若 metric 未显式处理范围错配，会把编译器产物错误当作 agent 失败。

**最小实验**：对同一源程序编译 O0/O2/O3，并加入一次内联、尾调用和函数合并变体。由专家标注二进制范围与源码语义对象的多对多关系；用单一函数配对与集合/图级配对各算一次 O、S+E。

**支持/反驳判据**：

- 支持：单一配对在优化变体的分数下降显著，但图级语义/证据保持；且专家认为主要是 oracle 错配；
- 反驳：单一配对与图级配对结论一致，或优化退化确实对应可观察语义错误；
- 约四分之一范围错配的文献观察值只能作为先验，不作为本 pilot 的强制阈值。

### D-D4：证据字段能区分“可运行代码”与“可审计理解”

**命题**：两个候选都通过行为测试时，要求地址、关键边、字段偏移和不确定性声明的 S+E 能区分可审计结果与偶然/过拟合结果；没有证据要求的 Re-Executability 排序不能稳定预测专家后续审计效率。

**最小实验**：选 3 个函数，准备行为相同但证据质量不同的候选：A 有正确地址/数据流，B 有部分错误地址，C 无地址但文本自然。让专家在限时内回答函数用途、关键副作用和修改位置。

**支持/反驳判据**：

- 支持：A 的专家正确率/时间显著优于 B/C，而三者 O 相近；S+E 比 Re-Executability 更能预测该排序；
- 反驳：证据字段不增加决策准确性，或专家无法可靠使用这些字段；
- 这是优先级很高但证据等级为 L3 的命题。

## 4. CREBench：可证伪命题

### D-C1：T1/T2 高分可能来自原型匹配或答案搜索

**命题（L1/L2）**：在标准算法、熟悉常量和固定字段格式上，T1/T2 会奖励原型猜测、训练记忆或搜索，而不一定反映 wrapper/数据流理解。

**最小实验**：构造 6 个变体：常量布局重排、等价 key/IV 存放位置、字节序/字段角色交换、未知或定制轮函数、去除算法字符串、加入无关 AES/DES 字符串。保持真实行为和 checker 不变；另加入一个与参考算法同名但行为不同的 decoy。

**支持/反驳判据**：

- 支持：T1/T2 在 decoy 或无关字符串上显著波动，T3 隐藏行为与地址证据不变或更能反映正确性；
- 反驳：在变体上 T1/T2 与 T3/专家理解保持同样排序，且错误原型能被证据检查及时惩罚；
- 记录猜测型 agent（只做字符串/签名搜索）作为 shortcut 基线。

### D-C2：T3 五个隐藏向量不足以证明 wrapper 等价

**命题**：五个隐藏向量可以验证有限行为，但不能稳定排除边界长度、异常路径、padding、随机/状态依赖上的错误实现。

**最小实验**：在现有五向量外加入长度分层、空输入、非对齐输入、错误 key/IV、重复调用状态和异常路径；对 wrapper 的输入输出建立属性 oracle，而非只比较单一密文。

**支持/反驳判据**：

- 支持：现有 T3 通过的候选在扩展族上出现系统性失败，且专家认为失败会影响兼容性/审计决策；
- 反驳：扩展测试几乎不改变候选排序，五向量覆盖已足够；
- 报告每个测试族的边际增益和测试成本，不能只报告合并平均值。

### D-C3：`pass@3` 的最高结果掩盖稳定性与成本

**命题**：取三次独立尝试最高分会夸大可复现能力，不能区分稳定的单次 agent 与反复试错后偶然成功的 agent。

**最小实验**：对同一题运行三次并记录每次 O、S+E、P、token、回合和 wall-clock；设置低/中/高预算。比较 max、mean、成功概率、success-at-budget AUC 和证据支持成功率。

**支持/反驳判据**：

- 支持：max 与 mean/单次成功率排序不一致，且高 max 但低稳定性的轨迹有更多无效 GDB 循环或无证据 claim；
- 反驳：三次结果高度一致，max 不改变 agent 排序；
- 正式报告必须保留 max 但禁止将其作为唯一主分。

### D-C4：证据要求能区分偶然 flag 命中

**命题**：T4 flag checker 的正确不等于算法、key/IV 或来源解释正确；加入地址/trace/重实现证据后，能够降低偶然命中和错误归因。

**最小实验**：保留原 T4，要求额外提交算法/key/IV/wrapper 四类 claim 的地址或动态证据；设置两个等价解密路径和一个可由搜索得到 flag 但错误解释的对照。由专家检查证据是否支持成功。

**支持/反驳判据**：

- 支持：原 T4 成功数高于“证据支持成功”数，且支持成功与专家可复核性更一致；
- 反驳：所有 T4 成功均有正确证据，或证据字段无法提高区分力；
- 不能因提交格式不一致惩罚行为等价路径，应允许规范化别名和多解。

## 5. MalEval：可证伪命题

### D-M1：AE-F1/SAS 不足以证明语义支持

**命题（L1/L2）**：引用存在且三元组匹配不代表引用函数真正支持 claim；SAS/AE-F1 高而 EAS 低的案例应系统存在。

**最小实验**：构造三类 APK：无关敏感 API、真实行为但支持函数在一跳之外、以及存在函数但只在不可达分支执行。保持 action–asset–target 词面尽量相近，增加动态 trace 作为外部 oracle。

**支持/反驳判据**：

- 支持：SAS/AE-F1 与动态真实性出现高分脱钩，EAS/地址路径检查能发现；
- 反驳：静态匹配已稳定预测动态支持，额外 EAS 不增加专家排序；
- LLM judge 的 EAS 需报告与专家双标注的一致性、阈值和置信区间。

### D-M2：静态 CIR 对反射、动态加载和 native bridge 的鲁棒性不足

**命题（L1）**：固定 CHA/BFS+CIR 前端会漏掉运行时行为；删除/替换高敏感 CIR 的 ΔS 不能代表真实关键证据覆盖。

**最小实验**：在 3 个基础 APK 上各加入反射调用、动态 dex 加载、JNI/native bridge、WebView JavaScript 和无关敏感 API；同时提供静态 CIR、增强调用图和动态 trace 三种输入条件。

**支持/反驳判据**：

- 支持：静态条件下 AE-F1/B-F1 高但动态 oracle 或专家触发判断低；增强输入只在相关变体上恢复性能；
- 反驳：静态 CIR 已足以稳定定位行为，动态信息无增益；
- 把“前端遗漏”与“agent 推理失败”分别标注，避免混为一个分数。

### D-M3：RQ 可能与行为正确性和处置可用性脱钩

**命题**：叙述连贯、目标识别和逻辑可推导的 LLM judge 分数，不能单独保证行为召回、类别正确或分析师能做出处置。

**最小实验**：制作四类报告配对：高 RQ/错误 verdict、高 RQ/正确 verdict、低 RQ/正确证据、低 RQ/缺证据。让 blinded 分析师完成“升级/关闭/需动态验证”三路处置及定位关键函数任务。

**支持/反驳判据**：

- 支持：RQ 与处置正确率或行为 F1 的相关性显著低于 S+E/关键错误率；
- 反驳：RQ 在跨样本、跨 judge 和专家任务上稳定预测决策质量；
- 将 RQ 作为辅助诊断，不把它替代证据召回和关键漏报惩罚。

### D-M4：taxonomy 和受控等价类会惩罚合理的新表达

**命题**：AE-F1/B-F1 依赖固定 MalRadar taxonomy 与等价类；taxonomy 外行为、不同攻击链分解或家族级真值会造成错误负分。

**最小实验**：由专家为 3 个样本各写两份等价报告：规范 taxonomy 版本和合理的新行为/不同链分解版本；再加入至少一个真实未覆盖行为。比较原始 metric、开放专家 rubric 和动态行为 oracle。

**支持/反驳判据**：

- 支持：原始 F1 对专家认可的等价报告显著惩罚，而开放 rubric 不惩罚；
- 反驳：受控词表已覆盖专家认为必要的表达，且与开放 rubric 排序一致；
- 不能把 taxonomy 外行为直接计为 agent 错误，应单列“oracle 未覆盖”。

## 6. 跨文献的最小 Metric 验证矩阵

| 检验目标 | Decompile-Bench | CREBench | MalEval | 预期能否自动化 |
|---|---|---|---|---|
| 语义等价/多解容忍 | 变量、CFG、库调用变体 | 等价 key/IV/wrapper 路径 | 同义行为/攻击链变体 | 大部分自动化，专家确认可接受等价 |
| 有限 oracle 过拟合 | 隐藏测试+属性测试 | 扩展向量+异常路径 | 动态 trace/触发条件 | 测试执行自动化，真值需人工审计 |
| 证据真实性 | 地址/边/字段偏移核验 | 地址/trace/来源核验 | SAS+EAS+路径/trace | 存在性自动化，语义支持半自动/人工 |
| 过程效率 | 编译/测试/工具日志 | GDB/静态动态切换日志 | CIR 查询/动态验证日志 | 日志统计自动化，策略质量人工 |
| 鲁棒性 | O0-O3、内联/拆分 | 常量变换、未知算法、decoy | 反射、动态加载、native | 变体执行自动化，覆盖质量人工确认 |
| metric 可复现性 | 复算相似度和 judge | 复算 hidden tests 与成本 | judge 校准、标注一致性 | 评分器自动化，校准需专家 |

### 6.1 判定规则

一个候选 metric 只有在以下条件同时满足时才进入正式 stack：

1. 对至少一个已知失败注入产生预期方向的显著变化；
2. 对语义等价变体不过度惩罚，或其惩罚能被专家明确认为合理；
3. 与专家盲评排序的相关性不低于同层已有 metric，且置信区间不完全依赖单一 judge；
4. 可由另一位研究者按公开版本、输入和 oracle 复算；
5. 成本、标注时间和 gaming 风险被记录，并没有因简单增加调用/文本而虚高。

若只满足自动化方便而不满足上述条件，metric 只能作为诊断，不得作为主分。

## 7. 最小 Pilot 方案

### 7.1 数据与任务配置

总规模建议 9 个二进制/应用，三类任务各 3 个：

| 任务 | 3 个样本的覆盖 | 关键 oracle |
|---|---|---|
| 源码恢复 | 简单 O0、优化/内联、测试易过拟合 | 隐藏属性测试、函数/边/字段标注 |
| 加密逆向 | 标准算法、常量/布局变体、定制/未知 wrapper | 多族输入、异常路径、算法/key/来源证据 |
| 恶意审计 | 纯静态 Dalvik、反射/动态加载、native/JNI 或 WebView | 专家行为证据、动态 trace、verdict/类别 |

每个样本保留原始版本和一个对照变体；先做小规模数据审计，确认变体没有改变目标真值之外的因素。

### 7.2 被测系统与人类基线

- Agent：1 个目标 agent，若资源允许增加 1 个不同工具策略的 agent；固定模型版本和提示词，禁止在结果期修改协议。
- 人类基线：1–2 名有二进制逆向经验的研究者，独立完成同一输出 schema；记录经验年限、工具熟悉度和用时。人类结果不作为绝对真值，而作为决策效度参照。
- Shortcut 基线：字符串/签名检索、只跑公开测试、只输出分类/flag 的最小系统，用于检查 metric 是否被廉价策略击穿。

### 7.3 预算档位

建议以每个样本的相对预算定义，避免模型 token 不可比：

| 档位 | 过程限制 | 用途 |
|---|---|---|
| 低 | 10 回合或基准成本的 0.25 倍 | 测首次定位和早期失败 |
| 中 | 20 回合或 0.5 倍 | 测常规分析效率 |
| 高 | 40 回合或 1 倍 | 测充分分析与收益递减 |

同时记录 token、wall-clock、工具调用数和外部检索；若实际基准上限不同，按比例换算并公开换算式。

### 7.4 每次运行记录的最小字段

`sample_id, variant_id, agent_id, budget, seed, outcome_subscores, semantic_items, evidence_items, unsupported_critical_claims, tool_calls, evidence_producing_calls, repeated_calls, first_verified_time, token, wall_clock, abstentions, errors, final_artifact_hash`。

### 7.5 专家标注

两名专家分别标注：

1. 任务结果是否足以支持目标决策；
2. 每个关键 claim 是否语义正确；
3. 引用证据是否真实、局部、足够；
4. 错误严重度（关键错误/一般错误/可接受不确定）；
5. 是否应合理 abstain；
6. 完成任务所需时间和后续复核时间。

对分歧保留原始标签，再由第三方或共同复核裁决。报告 Cohen's kappa 或 Krippendorff's alpha、分歧例子和裁决规则。

### 7.6 分析与交付图表

Pilot 最少交付以下图表/表格：

- O、S+E、P 随预算变化的曲线及 95% CI；
- 等价变体与失败注入的分数差；
- 专家排序与各 metric 的 Spearman/Kendall 相关；
- 关键错误率（虚构地址、错误路径、错误 verdict、错误 key/IV 来源）；
- 成功率—成本 Pareto 曲线；
- 自动评分与专家裁决不一致的逐例审计；
- 评分器版本、未公开参数和无法复算项清单。

不建议在 9 个样本上做显著性过度解释。Pilot 的主要产物是效应方向、失败案例和评分协议修订；若要比较模型，后续应扩大样本并预注册分析。

## 8. 预期结果与决策树

每个命题的结果采用三值结论：

- **支持**：预设效应出现，且专家/外部 oracle 同方向；
- **反驳**：预设效应未出现，或相反，且数据质量检查通过；
- **未决**：样本、专家一致性或 oracle 不足，不能据此保留/删除 metric。

决策树：

```text
metric 在失败注入上不降？ -> 不能测该失败，降为诊断或删除
metric 在等价变体上大降？ -> 检查 oracle；若专家不认可，判代理失效
metric 与专家排序脱钩？   -> 不作为主分，保留失败分析
metric 成本高但增益小？   -> 移入抽样审计/人工复核层
metric 可复算且增益独立？ -> 纳入对应 O、S+E 或 P 分项
```

## 9. 当前证据不足与必须由专家验证的内容

### 已有文献仍不能支持的强结论

1. **充分证据阈值**：没有证据说明多少地址、trace、测试或配置项才足以支持源码、密码或恶意处置结论。
2. **动态证据的必要性**：MalEval 的静态证据是否足以覆盖 SOC 决策，CREBench/Decompile-Bench 的动态测试覆盖到什么程度才有实际效度，均未确定。
3. **错误代价权重**：错误恶意 verdict、错误 key/IV 来源、漏洞语义错误和普通可读性错误的相对成本需要真实工作场景标定。
4. **工具过程的通用价值**：GDB 循环、搜索、静态—动态切换和调用次数哪些是生产率信号，哪些只是个人风格，尚无专家数据。
5. **人类基线与可接受等价性**：不同经验专家对“可读源码”“合理攻击链分解”“足够解释”的一致程度未知。
6. **LLM judge 的替代性**：MalEval 的 EAS/RQ 虽有有限人工核验，不能推出其可替代双人专家审计；Decompile-Bench 的 GPT-Judge 也不能作为安全语义真值。
7. **跨平台泛化**：Android Dalvik、x86/x64 C/C++ 和 CTF 密码二进制之间的结构差异，尚不足以证明 stack 在 ARM、native malware、协议固件或混淆软件上稳定。
8. **污染与搜索捷径**：公开算法名、GitHub 源码、CTF 题面和训练语料可能让高分脱离真实二进制理解；需要隔离搜索、记忆和工具访问条件。
9. **过程日志隐私与成本**：token、外部检索、商业 API、动态沙箱和人工复核的真实成本尚未纳入可比较的预算单位。

### 专家访谈/观察应直接询问的问题

- 在什么证据不足时，专家会停止推断并明确 abstain？
- 哪些错误会阻止补丁、解密、告警或处置，哪些只是降低可读性？
- 专家是否愿意为地址/trace 证据付出额外时间，多少时间可接受？
- 复杂函数、多对多内联和跨模块调用下，专家实际如何定义“正确对象”？
- 工具调用日志中哪些行为被认为是有效推进，哪些只是个人偏好？

## 10. 阶段 D 结论

阶段 D 将三篇论文的局限转成了 12 个可证伪命题：Decompile-Bench 4 个、CREBench 4 个、MalEval 4 个。它们共同检验四类风险：参考表示代理、有限 oracle 过拟合、证据链断裂、固定前端/预算导致的过程与鲁棒性误判。

当前最稳妥的研究结论是：

> **只有在语义等价变体不过度受罚、已知关键错误能被发现、结果与专家决策保持关系、且评分可复算时，某个 metric 才能进入正式 stack。**

因此下一步不是给 O、S+E、P 预设权重，而是执行 9 样本、三预算、agent/专家/shortcut 对照的 pilot。Pilot 的成功标准是发现哪些原语真正增加了专家可用性和失败区分力；未通过的原语应降为诊断或删除，而不是因为易自动化而保留。
