# 面向项目级二进制逆向 benchmark 的专家需求调研方案

## 1. 真实问题的重述

你要解决的不是“为三个论文各找一个更好的分数”，而是一个测量有效性问题：如何把真实逆向工程中的目标、判断、证据和工具协作，转化为可复现、可区分 agent 能力、且能自动或半自动验证的多层评估体系。

被测对象是工具辅助的项目级二进制 agent，而不是孤立的反汇编器或问答模型。因此，能力至少包括：在大规模 stripped/优化/混淆二进制中导航；恢复结构和语义；追踪行为、漏洞或加密逻辑；选择并解释工具调用；在证据不完备时管理不确定性；产出可审计的结论和报告。

三篇文献分别涉及源码恢复、加密系统逆向解析、恶意应用识别与证据固定。它们的 metric 可能测量不同层次的对象，不能直接横向比较。需要先判断每个 metric 对应的真实工作决策，再判断它测到的是结果、语义模型、证据、过程还是鲁棒性，最后分析它遗漏了哪些专家真正关心的能力。

一句话表述：

> 设计一套需求驱动、证据约束、工具过程可追踪的 metric stack，使 benchmark 测到真实项目级逆向能力，而不是文本相似、题库记忆或偶然猜对。

## 2. 初步方案是否可行

“由我在短时间内调研专家需求”是可行的，但只能定位为**快速证据综合（rapid evidence synthesis）**，不能宣称等同于专家访谈或田野观察。

可行之处：

- 我可以从论文、漏洞分析报告、恶意软件报告、补丁分析、逆向博客和工具文档中抽取反复出现的任务、决策、证据类型和失败代价。
- 可以把这些材料与 `metric-tasks.md` 的需求→决策→证据→可验证输出闭环对齐，快速形成可检验的专家需求假设。
- 可以用统一 rubric 深入拆解三篇论文的 metric：测量对象、真值来源、评分粒度、成本、可复现性、失效模式、gaming 风险和与真实工作的对应关系。

不能过度承诺之处：

- 文献和公开案例会偏向“可发表、可记录”的工作，遗漏默会知识、时间压力、团队协作和工具切换。
- 没有真实专家独立标注，就不能报告需求的普遍性或行业共识，只能报告“由公开证据支持的候选需求”。
- 三篇论文的实验结果不能直接证明其 metric 的生态效度；最多能支持对设计得失的分析。

因此，明日交付应明确证据等级：把结论分成“文献直接支持”“多案例交叉支持”“基于逆向工作流的合理推断”“待专家验证”，并将最后一类列为后续研究计划。这样报告即使不完美，也不会把推断伪装成访谈结论。

## 3. 最可行的工作流程

采用“需求主线、metric 辅助”的闭环：

`真实场景 → 专家决策 → 能力原子 → 可观察证据 → 结构化输出 → metric → 小规模验证 → 修订`

### 阶段 A：建立快速专家需求库

先覆盖四类工作：程序/加密功能理解、漏洞与数据流分析、恶意行为识别、证据固定与报告。每个案例只抽取五项：

1. 任务目标：专家最终要完成什么；
2. 决策：结果将支持什么行动；
3. 关键证据：地址、调用链、动态 trace、配置、IOC、补丁差异等；
4. 失败代价：漏报、误报、错误归因、不可复现或浪费预算；
5. 工具行为：搜索、交叉引用、调试、动态执行、脚本/符号执行及其切换。

输出一张“需求—证据矩阵”，并为每项标注证据等级和来源。

### 阶段 B：为三篇文献制作统一 metric card

每篇文献使用同一模板，避免只做叙述性摘要：

```text
任务/真实场景：
metric 测量对象与层级：结果 / 语义 / 证据 / 过程 / 鲁棒性
输出与评分单位：
真值或 oracle 来源：源码、动态执行、专家标注、属性断言
部分分与不确定性处理：
与专家决策的对应关系：
覆盖的能力原子：
未覆盖或错误代理的能力：
主要失效模式：编译优化、多解、环境依赖、标签偏差、污染
成本、可复现性与可扩展性：
gaming/shortcut 风险：
可复用设计与改进建议：
```

三篇文献的比较结论必须回答同一个问题：该 metric 是否能证明 agent 理解了二进制并建立了证据链，还是只证明输出与某种参考表示接近。

### 阶段 C：提出最小 metric stack

明日不宜设计完整平台，建议交付一个可检验的五层框架：

- **Task Outcome**：功能/漏洞/行为/解析目标是否验证成功；
- **Semantic Model**：函数边界、调用图、数据流、结构体或协议状态是否正确；
- **Evidence Groundedness**：claim 是否绑定真实地址、trace、字符串、API 或差异证据，惩罚虚构和关键无证据断言；
- **Process and Tool Use**：工具调用有效性、证据产出率、静态/动态切换、预算内成功；
- **Robustness and Reliability**：剥离符号、优化、混淆、环境变化下的性能下降、校准和合理拒答。

这五层不是要求三篇论文都具备，而是作为评价它们的参照系。已有 metric 可作为测量原语库和反例库，不能反过来决定 benchmark 要测什么。

### 阶段 D：把分析变成可检验命题

为每篇论文写 2–4 个可证伪命题，例如：

- 若 metric 只比较恢复源码文本，则语义等价但表述不同的结果会被错误惩罚；
- 若加密解析只看最终算法/密钥是否命中，则无法区分理解、搜索和偶然成功；
- 若恶意识别只算家族分类，则不能测量行为证据的完整性、定位准确性和可复现性。

每个命题都配一个最小验证方法：构造语义等价变体、替换优化等级、加入无关字符串、要求地址级证据、设置工具/时间预算，观察分数是否符合专家判断。

### 阶段 E：形成明日交付包

最小交付应包含：

1. 一页问题与范围界定；
2. 一张专家需求—决策—证据矩阵；
3. 三张统一 metric card；
4. 一张五层 metric stack 对照表；
5. 每篇文献的 2–4 个可证伪风险命题；
6. 一个后续 pilot 方案：3–5 个二进制、三类任务、1–2 个 agent、1–2 名人类基线、低/中/高预算；
7. “当前证据不足之处”清单，明确需要真实专家访谈或双人标注验证的内容。

## 4. 对三类文献的统一审查重点

### 二进制源码恢复

重点检查参考源码是否被错误当成唯一真值。文本相似度、BLEU、CodeBLEU 或 exact match 容易惩罚语义等价表达，也可能奖励表面相似。应追问是否有编译/执行/属性验证，是否在不同编译器和优化级别下稳定，以及它是否真的对应专家的程序理解目标。

### 二进制加密系统逆向解析

重点检查 metric 是否只测算法名称、密钥或最终识别结果，是否测到数据流、轮函数/状态机、输入输出关系、配置解析和证据链。加密逆向常有多种等价解释；应使用属性断言、差分执行或中间语义结构评分，并记录工具成本和偶然命中风险。

### 二进制恶意应用识别与证据固定

重点检查“分类正确”是否被误当成“分析完成”。专家需要行为能力、触发条件、IOC/配置、相关函数/API、时间线和可复现证据。应分别评估行为 precision/recall、证据支持率、虚构地址率、动态环境依赖、置信度校准和漏报/误报代价。

## 5. 最终判断

你的方向是可行的，且在时间有限时值得采用，但报告必须诚实地把它定义为“基于公开证据的专家需求初步模型”，而不是完成了专家需求验证。主线应是自需求向 metric；三篇论文调研用于提供测量原语、揭示代理指标的失败模式，并帮助确定哪些层可以自动评分。

最重要的研究标准是：每个 metric 都能回答“它服务哪个真实决策、依赖什么证据、可能被什么捷径攻破、如何用小实验证伪”。只要明日交付能做到这四点，并清楚标出待专家验证部分，就已经是一个可检验且有研究价值的阶段性成果。
